- pages_count = @pages.count
- @pages.each_slice(3) do |pages|
  .pages{style: ('height: 100%' if pages_count < 4)}
    - pages.each do |page|
      .page
        .wrapper
          = link_to page_path(page) do
            %h2= page.name
          = my_image_tag page.images.first, :full, alt: page.name, title: page.name
