- @site.pages.each_slice(3) do |pages|
  .pages
    - pages.each do |page|
      - if page.images.first
        .page
          .wrapper
            = link_to page_path(page) do
              %h2= page.name
            = my_image_tag page.images.first, :full, alt: page.name, title: page.name
