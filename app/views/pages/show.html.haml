= render 'layouts/nav', site: @site
- if @page.description.present?
  .col-md-4.message
    .description
      .description-width
        = @page.description_html.html_safe
  .col-md-8.images.rotation
    - if @image
      = render @image
- else
  - if @image
    = render @image
  - else
    .col-md-12.images
      Oups, this page is empty, but there is a menu if you scroll...
