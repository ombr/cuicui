.page
  .row
    .col-sm-3
      %h1= @site.name
      = simple_form_for @site,  html: {class: 'form-horizontal' } do |f|
        = f.input :name
        = f.input :description, as: :text, input_html: { rows: 10 }
        = f.submit class: 'btn btn-primary'
    .col-sm-9
      %h1 Preview
      %iframe{src: "#{site_path(id: @site)}", style: 'height: 800px; width: 100%;'}
